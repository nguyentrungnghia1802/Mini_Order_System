# Optional native-debug override. The base Compose file keeps application ports private.
services:
  postgres:
    ports:
      - "${POSTGRES_DEBUG_PORT:-5432}:5432"
  rabbitmq:
    ports:
      - "5672:5672"
